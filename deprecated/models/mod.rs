// mod aircraft;
// mod aircraft_data;
// pub use aircraft::Aircraft;
// pub use aircraft_data::AircraftData;

// to use aircraft models, uncomment above imports, and add these imports to main.rs:

// extern crate serde;
// mod models;
// use models::AircraftData;
// use serde::{Deserialize, Serialize};

// add the following to Cargo.toml:

// serde = { version = "1.0.104", features = ["derive"] }
// serde_derive = "1.0.104"

// these imports might all not be strictly necessary but I'm just too lazy to go back and check
