version: '3.8'
services:
  redis:
    image: eqalpha/keydb:latest
    ports:
      - 6379:6379
    volumes:
      - $PWD/keydb.conf:/etc/keydb/keydb.conf
  express:
    build: .
    depends_on:
      - redis
    environment:
      - KEYDB_HOST=redis
    ports:
      - 80:80